#+title: Contributing to firefox-cookies.el

Keep the public function independent of particular websites.  Cookie selection
must behave as Firefox would for a complete URL and must not guess a user's
profile or container.

Every behavior change needs an offline ERT test using a temporary database.
Tests must never inspect a contributor's real browser profile or print cookie
values.
